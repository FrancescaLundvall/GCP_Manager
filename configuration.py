# In production this should of course be set in an environment variable as recommended in google documentation
path_to_credentials = '<PATH_TO_CREDENTIALS>'

# Convenient for debugging entire projects
LOG_LEVEL = "INFO"

# This could also be set to an environment variable if desired in production environment
disk_id = "<DISK_ID>"